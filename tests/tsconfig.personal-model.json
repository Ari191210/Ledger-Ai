{
  "//": "M19. Separate outDir per suite — see tests/tsconfig.registry.json for why. lib/personal-model.ts and lib/event-contract.ts are both I/O-free (no imports, no clock, no network), which is what makes V.5 provable with no Supabase project in reach (U.3, the determinism boundary).",
  "compilerOptions": {
    "outDir": "../.test-build-personal-model",
    "rootDir": "../lib",
    "module": "es2020",
    "target": "es2020",
    "lib": ["es2022"],
    "moduleResolution": "node",
    "baseUrl": "..",
    "paths": { "@/*": ["./*"] },
    "strict": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "files": [
    "../lib/personal-model.ts",
    "../lib/event-contract.ts"
  ]
}

{
  "//": "M11-1 … M11-6. Separate outDir per suite — see tests/tsconfig.registry.json for why. Every module compiled here is I/O-free by construction, which is what makes the whole of V.4 provable with no Supabase project in reach (U.3, the determinism boundary): `lib/mistakes/engine.ts` and `lib/mistakes/types.ts` import nothing; `lib/mistake-severity.ts` and `lib/mistake-retest.ts` import only the engine; `lib/mistakes/store.ts` adds those two and takes its database as injected verbs (`MistakeDnaDb`), never as a `SupabaseClient`. `lib/event-contract.ts` is here because V.4.4's THIRD refusal lives in it.",
  "compilerOptions": {
    "outDir": "../.test-build-mistake-dna",
    "rootDir": "../lib",
    "module": "es2020",
    "target": "es2020",
    "lib": ["es2022"],
    "moduleResolution": "node",
    "baseUrl": "..",
    "paths": { "@/*": ["./*"] },
    "strict": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "files": [
    "../lib/mistakes/store.ts",
    "../lib/mistakes/engine.ts",
    "../lib/mistakes/types.ts",
    "../lib/mistakes/migrate-legacy.ts",
    "../lib/mistake-severity.ts",
    "../lib/mistake-retest.ts",
    "../lib/event-contract.ts"
  ]
}

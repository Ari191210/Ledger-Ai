{
  "//": "M21. Separate outDir per suite — see tests/tsconfig.registry.json for why. lib/today/*.ts is I/O-free (no imports beyond lib/recommendations/types.ts's EvidenceRef, no clock, no network), which is what makes L.3/L.4/V.7 provable with no Supabase project in reach (U.3, the determinism boundary).",
  "compilerOptions": {
    "outDir": "../.test-build-today",
    "rootDir": "../lib",
    "module": "es2020",
    "target": "es2020",
    "lib": ["es2022"],
    "moduleResolution": "node",
    "baseUrl": "..",
    "paths": { "@/*": ["./*"] },
    "strict": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "files": [
    "../lib/recommendations/types.ts",
    "../lib/today/types.ts",
    "../lib/today/engine.ts"
  ]
}
